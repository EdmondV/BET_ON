/**
 * A link to a certain page, an anchor tag
 */

import { keyframes } from 'styled-components';

const slideRight = keyframes`{ from {opacity: 0;} to {opacity: 0.75;} }`;

export default slideRight;
